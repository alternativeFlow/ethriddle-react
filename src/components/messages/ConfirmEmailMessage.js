import React from 'react';
import { Alert } from 'react-bootstrap';

const ConfirmEmailMessage = () => (
	<Alert bsStyle="info">
		Please verify email to unlock
	</Alert>
);

export default ConfirmEmailMessage;